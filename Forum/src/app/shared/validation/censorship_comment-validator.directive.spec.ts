import { CensorshipCommentValidatorDirective } from './censorship_comment-validator.directive';

describe('CensorshipCommentValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CensorshipCommentValidatorDirective(null);
    expect(directive).toBeTruthy();
  });
});
