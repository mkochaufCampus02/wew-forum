import { NewPasswordDirective } from './new-password.directive';

describe('NewPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new NewPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
