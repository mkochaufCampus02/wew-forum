import { CensorshipValidatorDirective } from './censorship-validator.directive';

describe('CensorshipValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CensorshipValidatorDirective(null);
    expect(directive).toBeTruthy();
  });
});
